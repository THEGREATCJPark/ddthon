# P1 lifecycle acceptance continuation

Approved by user in current conversation before execution. This extends existing scenario-correction step 6 with three additional Cold acceptance runs, not product scope.

1. Record the exact human-approved ref without changing content; independent Replay on the different-layout open workbook. Publish only after actual PASS.
2. GitHub team-skill-store actual push, then import into a separate Warm store/mirror. Do not fabricate receiver review/Replay. New Agent uses actual user authorization for this exact digest only.
3. Three additional fresh Claude Cold sessions: isolated stores contain P0 only, encrypted workbooks already open, different sheet/row/column/value profiles. User prompt only requests production forecast. No solution or task mapping in context. Preserve failures/retries and classify actual results. No product changes during these runs.
4. Check original hashes, chart/result values against operator-only profiles, candidate excludes task details, Cold reuse=0/candidate=1, Warm reuse=1/candidate=0. Share actual Warm event, import twice and verify dedup.
5. Save sanitized actual tool traces (no thinking/signatures), result summaries, B setup guide and team message. Distinguish one-PC separate-workspace transport proof from B-PC reproduction tomorrow. S3/database remain architecture possibilities, not implemented adapters.

Existing fixed-seed regression 139 PASS/2 SKIP and separate Excel PASS are baseline; repeat only if code changes or a failure requires it. CI run 34235737366 success confirmed by API. Active partial PBT preserved, no new pure algorithms. Performance N/A (no approved quantitative target); public data limited to descriptors and verified usage. Official rules unchanged.
