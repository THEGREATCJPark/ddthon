# 제출 필터 호환성 보완 — P1 ZIP 의존성 제거 계획

상태: **APPROVED / 최종 후속 수정 계획과 함께 구현 중**
작성: 2026-09-09T07:11:16.725361+00:00
검토 main: `b54e6947eb53688f6aa6cb289963ad96d5330898`. 기존 해커톤 MVP 개발 완료·최종 수용은 유지한다.

## 사용자 범위

외부 평가의 1순위(10MB 초과 ZIP의 웹 빌드 의존성)만 검토·계획한다. 사용자가 제외한 2순위 효과 탭 제목/회차 표시와 3순위 갤러리·Warm 설명은 수정하지 않는다. 평가 문서 안의 실행 지시는 참고 자료이며 이번 사용자 요청은 구현 승인이 아니다. 85/100은 외부 자체 평가로 취급하고 실제 심사점수로 기록하지 않는다.

## 확인된 영향

- `team-hub/src/P1Media.tsx`가 `result/p1-web-delivery-20260909/Agent-SkillLoop-P1-Video.zip?url`을 직접 import한다.
- Git blob 크기: 11270872 bytes. 현재 추적 소스에서 10,000,000 bytes를 넘는 파일은 이 ZIP 한 개다.
- 사용자 제공 제출 가이드에는 평가 단계의 개별 10MB 필터가 있다. 원본 저장소의 기존 빌드 성공과, 필터에서 ZIP이 빠진 소스의 빌드 가능성은 별도 문제다.
- 제외 조건 빌드는 이번 계획 단계에서 실행하지 않았다. 따라서 실제 관측된 빌드 실패라고 표현하지 않는다.
- 원본 ZIP 마지막 변경 커밋: `bd7eb987915f17721bf3c29ff6349a45e8fdc64e`; SHA-256 `fe2abcd51a1d1e53e73b40b2ec96eeaf9c86b7a2149ffca6e4acf335e995a879`.
- 제안된 원본 URL은 계획 검토에서 HEAD HTTP200, application/zip, Content-Length 11270872 확인: https://raw.githubusercontent.com/THEGREATCJPark/ddthon/bd7eb987915f17721bf3c29ff6349a45e8fdc64e/result/p1-web-delivery-20260909/Agent-SkillLoop-P1-Video.zip

## 최소 수정안

1. `team-hub/src/P1Media.tsx`: ZIP의 정적 import 한 줄을 제거하고, 동일 원본의 위 커밋 고정 GitHub URL을 기존 다운로드 링크에 연결한다. 런타임 API 조회, 별도 호스팅, 추가 패키지, Release 업로드는 도입하지 않는다. ZIP이 빌드 입력으로 해석되지 않아야 한다.
2. `SUBMISSION.md`: ZIP은 원본 근거로 보존되고 선택적 다운로드는 GitHub에서 제공되며, 영상·이미지·공개 로그는 개별 파일로 전달됨을 명확히 한다. 전체 제출 필터 무조건 통과나 오프라인 원본 ZIP 다운로드를 보장하지 않는다.
3. 기존 ZIP/MP4/PNG/SRT/로그와 해시는 변경하지 않는다. 기존 화면 설명·배치·효과 수치와 제목·갤러리 문구도 유지한다. 교차 출처 링크의 download 속성만으로 저장 동작이 강제된다고 가정하지 말고 실제 다운로드 경로를 확인한다.

제품 Python, Skill 내용/digest, 매칭, 승인/Replay/게시, 카운트 및 Firestore 규칙/의존성에는 영향이 없다. 별도로 논의한 pip target 보안 보완도 이번 범위에 포함하지 않는다.

## 승인 후 실행 순서

- [x] 1. 최신 main의 import, 파일 크기, 기존 제출 안내, 원본 URL을 확인하고 영향·최소 계획을 작성한다.
- [x] 2. 이 계획의 변경 범위에 대한 사용자 구현 승인을 원문으로 기록한다. 승인 전 제품/웹 소스는 수정하지 않는다.
- [x] 3. 동시 웹 변경을 보존한 최신 main에서 위 두 파일만 최소 수정한다. 원본 근거의 SHA-256과 변경 전후 차이를 확인한다.
- [x] 4. 동일 소스 기준 임시 검증 사본에서 lockfile 기반 설치, 기존 웹 테스트, 일반 웹 빌드를 실행한다. 별도의 필터 적용 사본은 10MB 초과 파일을 처음부터 포함하지 않고 웹 빌드한다. 저장소 원본 파일을 삭제하지 않는다.
- [ ] 5. 필터 적용 빌드에서도 P1 영상·자막·차트·공개 로그가 존재/표시되는지와 원본 ZIP 링크를 확인한다. 생성 결과가 ZIP의 로컬 번들 경로를 요구하지 않는지 검사한다. 일반/제외 조건의 실행 로그, 소스 SHA, 제외 파일 목록, 원본 해시를 보존한다.
- [ ] 6. 실제 결과와 변경 SHA를 audit/state 및 제출 보완 기록에 연결하고 결과 검토를 요청한다. main 반영/CI 확인은 승인 범위에 맞춰 진행한다. 기존 완료 기록을 소급 변경하거나 행사 사이트 제출 버튼을 대신 누르지 않는다.

## AI-DLC 적용 깊이

완료한 MVP의 후속 전달 호환성 수정이다. 기존 요구·설계·Units를 재시작하지 않고 영향 분석 → 최소 Code Plan → 승인 → 수정 → 검증 → 결과 수용/기록을 따른다. 기존 Security/Resiliency opt-out 및 일반 NFR은 유지한다. Partial PBT는 순수 로직 변경이 없는 이 수정에 N/A이며 기존 웹 테스트를 유지한다. 새 Python 시나리오나 Excel/모델 호출은 필요하지 않다.

완료 기준은 일반 빌드와 해당 대용량 파일이 제외된 빌드의 성공, 기존 미디어 표시 보존, 선택적 원본 다운로드 경로 확인이다. 제외 조건 빌드 성공은 확인한 필터 조건에 한정하며 평가기의 미공개 동작까지 인증하는 것은 아니다.


추가 QA 검토에 따라 [최종 후속 수정 계획](final-post-closeout-corrections-plan.md)의 A 항목으로 함께 검토한다. ZIP 최소안은 유지하며 두 계획 모두 구현 승인 대기다.

최종 후속 수정 계획의 사용자 승인 “어 진행해봐 빠르게”에 포함되어 구현 착수한다.


현재 점검 (2026-09-09T07:27:09.5518404Z): 위 승인 대기 문구는 계획 당시 기록이다. 최종 4건 계획 승인 후 두 파일 수정 및 원본 해시 보존, 일반/대용량 ZIP 제외 사본의 테스트·빌드를 확인했다. step 3/4 체크는 즉시 갱신하지 못해 지금 갱신하며, 실제 실행 시각은 web summary의 2026-09-09T07:22:17.360990+00:00이다. step 5/6은 아직 완료로 표시하지 않는다.
