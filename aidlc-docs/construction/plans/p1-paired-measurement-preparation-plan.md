# P1 paired measurement preparation — approved bounded work

## Authority and phase

Continue CONSTRUCTION / integrated Build & Test. User approved the previously presented preparation sequence with: “그렇다면 진행해. 테스트 이행 전까지 구성완료를 목표로”. This records that current approval before implementation; it does not invent an earlier Code Plan approval. No model invocation, new product scope, publication, web work or historical-result correction is authorized by this step.

Traceability: US-P1-1/2 (access and task result), US-P1-4 (existing exact Skill provenance), existing operator/org and P1 filming plans. This is a comparison infrastructure addition, not a change to those product contracts. Full-product filming remains separate from NO_SKILL versus WARM_SKILL_ONLY.

## Design and NFR delta

- Input: received P1-Excel-Handoff-20260909.zip; verify every manifest payload before staging. Never execute the supplied paused/historical runner or import its old reference implementation. Preserve the original ZIP. Operator oracle and provenance remain outside Agent workspace and outside public result bundles.
- Shared preparation: exact v2 encrypted inputs and original task/boundary; Excel preopened read-only before each timed run. New study ID and new six runs, never append to historical v2. Common software conditions are proposed, not silently relaxed to this PC's installed versions.
- Isolation: Agent must not terminate external recorder or access recorder/oracle files. Separate PID, prompt, or same-user directory is insufficient. This non-elevated Windows Home session has no confirmed isolated account/VM. Do not create accounts, install a hypervisor, or claim containment without verified setup. A recorder bridge must retain events and final/partial state outside the Agent security principal. OS provisioning and other-PC receipts remain explicit readiness prerequisites.
- Skill: preserve product descriptor and original human review/Replay/publication evidence. A standalone textual procedure is a DERIVED artifact, not the original descriptor. Validate on separate setup data; exact measurement inputs must not be used for developing the procedure. No task answers, fixed sheet/columns, passwords, or machine paths in injected text. Derived text is not automatically covered by the original publication approval.
- Verification: independent row and forecast arithmetic; unknown prediction methods require review rather than retroactive OLS assumptions. Chart byte validity is distinct from semantic visual review. Partial output or missing final usage never becomes success/zero. External access/safety review remains required.
- PBT extension remains scoped to existing product invariants; preparation tests cover manifest tampering, path traversal, wrong/partial task answers, nonfinite values, invalid chart, missing safety/evidence, interrupted usage and readiness failures. Existing security/resiliency extension choices unchanged.

## Code Plan

1. [x] Create `scripts/p1_comparison.py`: safe handoff validation/staging, machine inventory, draft shared protocol, fail-closed readiness, independent result verifier. No live model command is enabled by this preparation tool.
2. [x] Create `scripts/p1_comparison_skill.md`: derived portable read-only Excel access procedure; preserve original source/provenance hashes outside the Agent workspace. Verify against a newly generated non-measurement encrypted workbook using the current product executor and separate read-only access recipe. This is mechanical procedure validation, not Agent treatment validation.
3. [x] Create `tests/test_p1_comparison.py` with negative and interrupted cases; run only preparation tests. Capture actual setup evidence and machine/version gaps in `result/p1-comparison-preparation-20260909/`.
4. [x] Stage a new operator kit outside the repo; exact input files and task/boundary are portable. No automatic Excel opening, key guessing, old runner reuse, model call, or prefilled PASS. Prepare peer handoff instructions and conditional receipt templates.
5. [ ] Update audit/state and delivery guide with actual achieved readiness. Protect user filming folder and existing runs. If keys, OS isolation, common runtime, peer agreement, recorder integration or independent chart review are unavailable, report them explicitly; do not label PREPARED artifacts READY_TO_RUN.

## Completion boundary

Deliver executable offline preparation and verification tooling, tests, operator kit and reproducible readiness report. Actual measurement readiness additionally requires external recorder/isolation implementation and proof, portable operator key preparation, approved derived treatment evidence, matched runtimes, both-PC common freeze. These are not satisfied by this document or by manifest tests. User authorization stops before Cold/Warm model calls.

## Actual preparation outcome

Offline tooling and operator kit implemented; 20 preparation tests PASS. A new encrypted setup workbook (not received measurement input) passed real Excel read-only attach/value/hash/mtime checks. Derived text is mechanically validated, not model-validated or newly published. Portable doctor intentionally exits 2 PREPARED_NOT_READY. No live runner was activated and no model called.

Step 5 remains incomplete at its readiness boundary: protected recorder/OS isolation and its live bridge are not implemented here; source input keys are absent; Python/dependency versions differ; both-PC acceptance/freeze is pending. Windows Home session is not elevated; no system account or VM was provisioned. Local scripts do not replace those missing steps with self-attestation. Delivery: Desktop/P1-Paired-Preparation-20260909.zip, operator-only (includes oracle, excludes keys). Source product/web/filming state unchanged.
