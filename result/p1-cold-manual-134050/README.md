# P1 Cold 실제 사용자 실행 측정 — 2026-09-09

조건: FULL_PRODUCT_COLD_MANUAL. 세션 `e518ab42-7679-4726-849b-252f64fbcd99`.
시작 전 P1 Skill 없음. 사용자가 도중에 대상 Excel을 열고 환경 사실을 전달했다.

## 결과와 시간

| 구간 | 실제 경과 |
| --- | ---: |
| 업무 요청 → 최초 WORK_COMPLETE(계산·차트) | **156.826초 (2분 36.8초)** |
| 업무 요청 → 사용자에게 업무 결과 설명 | 175.737초 (2분 55.7초) |
| 업무 요청 → 원격 게시 완료 | 228.255초 (3분 48.3초) |
| 업무 요청 → 최종 답변 | 240.735초 (4분 0.7초) |

주 측정 기준은 첫 번째 구간이며 사람 승인·Replay·게시를 제외한다. 사용자 환경 응답 대기 19.420초는 포함한다. 순수 모델 처리시간이나 CLI의 활동시간 표기와 같지 않다.

- 업무 PASS: 실적 1200/1350/1500, 다음 달 예측1650. 실제 생성 차트에서 실적과 예측 구분을 확인했다.
- 실제 사용자 선택으로 사람 승인, 독립 Replay PASS, Git 게시 PUBLISHED 확인.
- 원격 `codex/p1-cold-ready-r1-20260909-134050`의 `e302bf213679a038fea04e5058188096cb96c24b`가 게시 기록과 일치한다.
- Cold 후보 +1, 실제 재사용 +0. 기존 Skill을 재사용한 Warm 결과가 아니다.

## 토큰

| native usage 항목 | 업무 결과까지 | 사용자 결과 설명까지 | 게시 후 최종 답변까지 |
| --- | ---: | ---: | ---: |
| input_tokens (캐시 외) | 28 | 32 | 46 |
| output_tokens | 6,950 | 8,152 | 10,628 |
| cache_creation_input_tokens | 24,463 | 26,890 | 32,841 |
| cache_read_input_tokens | 451,854 | 532,610 | 844,565 |

Claude 원본 JSONL에서 message.id별 사용량을 한 번씩 집계했다. 캐시 읽기는 반복 문맥을 포함하며 새로 생성된 출력 토큰이 아니다. 과금 단가가 다른 항목을 단일 과금 토큰으로 합산하지 않는다. 공급자 청구서 또는 전체 세션 /cost 측정값은 아니다. 사용자 결과 설명에는 후보 안내가 함께 있어 그 메시지의 토큰을 문장별로 분리하지 않았다.

## 보존한 시행착오와 제한

- 여러 Excel 인스턴스 검색과 시트 이름 UTF-8 재확인이 있었다.
- 최초 Replay 명령은 --xlsx 누락으로 종료2, 인자를 넣은 재시도는 PASS. 성공한 부분만 편집하지 않았다.
- Replay는 동일 접근 절차를 새로 실행하는 검증이며, Agent의 ‘다른 접근’ 설명을 사실로 채택하지 않는다.
- 같은 폴더의 앞선 중단 세션 b1770452-1aaf-4573-bc30-deceb5aa5514는 별도 보존하고 이번 합계에 섞지 않았다.
- 이번 기록은 성공한 Cold 1회다. 3회 성공률이나 다른 PC 대비 Skill만의 성능 개선율을 주장하지 않는다.
- 현재 이 작업 폴더와 브랜치에는 P1 Skill이 게시돼 있으므로 다시 Cold라고 사용하지 않는다.

## 증거

- [측정 JSON](measurement.json), [CSV](metrics.csv), [실제 차트](actual-trend.png)
- 운영자 원본 스냅샷: `C:/Users/cik61/Desktop/skillloop-p1-cold-ready-20260909-134050/operator/completed-run-measurement/transcript-snapshot.jsonl`
- 원본 스냅샷 SHA256: `44ffdb2249004832ee81b3ed9753b5dec90be36cf92b5929145cf48575ae5c8f`
- 원본 대화 전체는 운영자 폴더에 보존하며 공개 결과 폴더에 복제하지 않았다.

AI-DLC: 승인된 CONSTRUCTION / Build & Test의 실제 수용 결과 수집이다. 제품 코드·공식 규칙·게시 게이트를 변경하지 않았다. 이 결과로 전체 프로젝트 완료나 과거 오류 해소를 소급 선언하지 않는다.
