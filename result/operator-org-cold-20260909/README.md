# Operator + Git organization demonstration evidence

Continue approved AI-DLC CONSTRUCTION / integrated Build & Test. No website changes.

- operator-open-proof.json / .py: real Excel closed/reopen for two protected workbooks, no password input, exact readonly values and original hashes. Direct parser BadZipFile. This is COM execution evidence, not an independent human screen inspection. Earlier failed reopen receipts in other result directories remain unchanged.
- p0-bootstrap-replay.txt: actual independent pip install/version/import, clean venv, no reuse event; exact candidate reviewed by the user through the initial native prompt before their subsequent UI correction.
- p0-bootstrap-publish.txt: actual live Git publication. Live data branch team-skill-demo-20260909, initial commit75b6e54225a9fa4e71e7174168161d61fa923e31. P0 only, no P1 data or fabricated reuse.
- prepare-p0.txt / prepare-p1.txt: real live workspaces. The P1 preparation log was generated before the operator help message changed to Open-cold.cmd; launchers were subsequently written to the actual root. Preserve original log.
- organization-preflight.json / .txt / .py: real Git P0 receipt, denied unconfirmed execution, actual confirmed install/import, one event and repeat no increment in a separate temporary work folder. No preflight event pushed to the live branch. Live P0 events0; P1 Cold/Warm readiness reads do not create candidates or approvals.
- p0-status.txt / p1-status.txt: actual initial status snapshots (refresh after final integration as needed).
- initial-targeted.txt and targeted.txt: intermediate test coverage, not final source claims.
- conversation-targeted.txt: first receipt test failure used a stale cached store object after CLI wrote its own instance; corrected fresh-read assertion. On-disk exact-candidate check also added and tested.
- full-regression.txt: superseded full run overlapped user's explicit GUI-removal instruction; old collected GUI test import failed after its module was removed. 163 passed /1 failed /2 skipped; do not present this as final PASS.
- final-targeted.txt:47 passed at that intermediate source. Final full run and any subsequent scoped checks have their own receipts.

Final product flow uses Claude Code conversational permission, NOT a Windows GUI. A trusted Agent relays an actual user answer with exact candidate/reviewer/source into the CLI; this is recorded provenance, not independent identity authentication. No Agent-generated yes or automatic candidate approval. The final live P1 discovery/publication remains for the user.

Final result: final-full-regression.txt =165 passed/2 skipped (277.06s); final-setup-check.txt =15 passed. These counts overlap; do not add them. run-manifest.json records exact local setup and code hashes. Live user Agent approval dialogue/captures and subsequent P1 publication are pending.
